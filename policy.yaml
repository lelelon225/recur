on_http_request:
  - actions:
      - type: oauth
        config:
          provider: google

  - expressions:
      - "actions.ngrok.oauth.identity.email != 'leon.hebeisen08@gmail.com'"
    actions:
      - type: deny